# ── Core ML ──────────────────────────────────────────────────────────────────
# On CSC Roihu: `module load python-pytorch`, then create the venv with
# `python -m venv --system-site-packages pvr_env` so torch/torchvision (and
# their CUDA stack) come from the module, not pip. The pins below match what
# the module currently provides -- keep them in sync, or pip will try to
# reinstall/downgrade torch inside the venv instead of reusing the module's
# build.
torch==2.12.1
torchvision==0.27.1

# ── Environments ─────────────────────────────────────────────────────────────
gymnasium==1.3.0
gymnasium-robotics==1.4.2
minigrid==3.1.0
ale-py==0.12.0
shimmy[dm-control]==2.0.1
mujoco==3.8.1
opencv-python-headless==5.0.0.93
# CarRacing-v3 (configs/env/carracing_pixels.yaml) needs Box2D/box2d-py --
# not present in the environment this file was generated from. Untested;
# add box2d-py here once confirmed working on this platform.

# ── Vision models ────────────────────────────────────────────────────────────
timm==1.0.25
git+https://github.com/openai/CLIP.git
open_clip_torch==3.3.0
git+https://github.com/facebookresearch/r3m.git
git+https://github.com/facebookresearch/vip.git
# LIV embedding only — two-step install, doesn't fit a single pip line,
# install separately if needed:
#   pip install git+https://github.com/penn-pal-lab/LIV.git
#   (then, from the cloned repo) cd liv/models/clip && pip install -e .
# MaskRCNN embedding only — large build, install separately if needed:
# git+https://github.com/facebookresearch/detectron2.git

# ── RL backend / experiment tracking ────────────────────────────────────────
stable_baselines3==2.9.0
wandb==0.25.0
tensorboard==2.20.0
protobuf==5.27.2          # tensorboard/wandb both depend on this; pinned
                          # explicitly to avoid the two resolving to
                          # incompatible versions

# ── Offline RL datasets ───────────────────────────────────────────────────────
minari==0.5.3

# ── Config & experiment management ───────────────────────────────────────────
hydra-core==1.3.2
omegaconf==2.3.0

# ── Utils ────────────────────────────────────────────────────────────────────
numpy==2.4.4
Pillow==12.2.0
tqdm==4.68.1
pandas==3.0.3
matplotlib==3.10.8
seaborn==0.13.2
submitit==1.5.4
GitPython==3.1.46
